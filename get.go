package sessionfrontend

import "github.com/cdvelop/model"

func (a sessionFrontend) GetLoginUser(http_request any) (u *model.User, err string) {

	return nil, "error authfrontend GetLoginUser. usuario no registrado"
}
